import styled from 'styled-components';
import tw from 'twin.macro';

export const HousingListContainer = styled.div`
  ${tw`flex flex-wrap sm:max-w-screen-xl w-full mx-auto p-5 pb-20`};
  min-height: calc(100vh - 142px);
`;
