import React from 'react';
import * as Styled from './styles';

const HousingListCardContainer = ({ children }) => (
  <Styled.HousingListCardContainer>{children}</Styled.HousingListCardContainer>
);

export default HousingListCardContainer;
