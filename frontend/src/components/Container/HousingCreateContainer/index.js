import React from 'react';
import * as Styled from './styles';

const HousingCreateContainer = ({ children }) => (
  <Styled.HousingCreateContainer>{children}</Styled.HousingCreateContainer>
);

export default HousingCreateContainer;
